db.createView(
  "sales_summary",
  "inventory",
  [
    {
      $lookup: {
        from: "sales",
        localField: "name",
        foreignField: "medicine",
        as: "salesData"
      }
    }
  ]
);